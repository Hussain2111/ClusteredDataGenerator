{
  "name": "imran-internship",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "build": "tsc",
    "generate": "npm run build && node dist/generate.js",
    "cluster": "npm run build && node dist/cluster.js",
    "generate-ts": "ts-node generate.ts",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@faker-js/faker": "^8.4.1",
    "exceljs": "^4.4.0",
    "xlsx": "^0.18.0"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "ts-node": "^10.9.2",
    "typescript": "^5.3.3"
  }
}
